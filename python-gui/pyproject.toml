# python-gui/pyproject.toml
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "chat-client"
version = "2.0"
requires-python = ">=3.13"
# dependencies = ["requests", "httpx"]  # add yours here
